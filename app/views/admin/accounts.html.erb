<h2> Accounts </h2>
<% @accounts.each do |account| %>

<div class="row">
    <div class="col-sm-3">
        <%= account.full_name %>
    </div>
    <%# <div class="col-sm-3"> %>
        <%# <%= account.email %> %>
    <%# </div> %>
    <%# <div class="col-sm-3"> %>
        <%# <%= account.created_at.strftime("%d %m %Y")%> %>
    <%# </div> %>
    <div class="col-sm-3">
        <%= link_to "View Profile", profile_path(account.id) %>
        <%# <%= button_to "Delete", accounts_path(account.id), method: :delete, data: { confirm: "Are you sure you want to delete this account?" }, class: "btn btn-danger" %>
        <%# <%= button_to "Delete", admin_account_path(account), method: :delete, data: { confirm: "Are you sure you want to delete this account?" }, class: "btn btn-danger" %>

    </div>

</div>
<%end%>
